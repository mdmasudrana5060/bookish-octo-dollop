const loginPage = () => {
  // use react hook from call userLogin.tsx from service/actions
  // if res.data.accessToken then call storeUserToken from service/authService
  return (
    <div>
      <h1>login page</h1>
    </div>
  );
};
export default loginPage;
